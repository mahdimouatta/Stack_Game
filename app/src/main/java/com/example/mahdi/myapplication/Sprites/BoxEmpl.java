package com.example.mahdi.myapplication.Sprites;

import com.example.emobadaragaminglib.Base.Game;
import com.example.emobadaragaminglib.Base.Image;
import com.example.emobadaragaminglib.Components.Sprite;

public class BoxEmpl extends Sprite { // box emplacement : c'est la zone ou notre box doit etre placé

    public BoxEmpl(Game game, Image image, int x, int y, int height, int width) {
        super(game, image, x, y, height, width);
    }
}
